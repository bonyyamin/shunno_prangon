// lib/features/profile/presentation/pages/saved_articles_page.dart
import 'package:flutter/material.dart';

class SavedArticlesPage extends StatelessWidget {
  const SavedArticlesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Saved Articles')),
      body: const Center(child: Text('Saved Articles - Coming Soon')),
    );
  }
}